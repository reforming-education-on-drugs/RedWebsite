{
  "scripts": {
    "install": "concurrently -r \"cd Frontend && npm i\"  \"cd backend && npm i\"",
    "start": "concurrently -r \"npm start --prefix Frontend\" \"npm start --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
